# summary

Display the current CLI environment context.

# description

Show the active target org, dev hub, API version, project information, authenticated orgs, and CLI version. Always outputs JSON for programmatic consumption.

# examples

- <%= config.bin %> <%= command.id %>
